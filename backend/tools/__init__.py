"""
Standalone-скрипты для разработки и отладки (НЕ часть продакшн-кода).

* `live_smoke.py` — проверка adapter-слоя на реальном Tinkoff-токене из
  `.env.local`. См. план «Live Smoke» в plans/.

Запуск: `cd backend && python -X utf8 -m tools.live_smoke --mode quick`.
"""
