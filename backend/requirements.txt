# ATOM Backend Dependencies
# Python 3.11+

# ==================== WEB FRAMEWORK ====================
fastapi>=0.109.0,<0.115.0
uvicorn[standard]>=0.27.0,<0.32.0
python-multipart>=0.0.9  # File uploads

# ==================== DATABASE ====================
sqlalchemy>=2.0.0,<3.0.0
alembic>=1.13.0,<2.0.0

# SQLite (built-in)
# PostgreSQL support
psycopg2-binary>=2.9.9,<3.0.0  # или asyncpg для async

# ==================== AUTHENTICATION ====================
python-jose[cryptography]>=3.3.0,<4.0.0  # JWT
bcrypt>=4.1.0,<5.0.0  # Password hashing
passlib[bcrypt]>=1.7.4,<2.0.0  # Password utilities

# ==================== ENCRYPTION ====================
cryptography>=42.0.0,<44.0.0  # Fernet encryption for broker tokens

# ==================== HTTP CLIENTS ====================
requests>=2.31.0,<3.0.0
httpx>=0.26.0,<0.28.0  # Async HTTP client for OAuth

# ==================== DATA PROCESSING ====================
pandas>=2.2.0,<3.0.0
numpy>=1.26.0,<2.0.0
openpyxl>=3.1.2,<4.0.0  # Excel files

# ==================== RATE LIMITING ====================
slowapi>=0.1.9,<0.2.0
redis>=5.0.0,<6.0.0  # Optional: for distributed rate limiting

# ==================== UTILITIES ====================
python-dotenv>=1.0.0,<2.0.0  # Environment variables
pytz>=2024.1  # Timezone support
email-validator>=2.0.0,<3.0.0  # для pydantic.EmailStr

# ==================== PRODUCTION RUNTIME ====================
gunicorn>=21.2.0,<23.0.0  # WSGI manager + uvicorn workers (см. Dockerfile CMD)

# ==================== OBSERVABILITY ====================
sentry-sdk[fastapi]>=2.0.0,<3.0.0  # Error tracking; opt-in через SENTRY_DSN
python-json-logger>=2.0.7,<3.0.0  # JSON логи для prod-агрегаторов

# ==================== BROKER INTEGRATIONS ====================
# Tinkoff Invest API — официальный gRPC SDK (greenfield rewrite, см. PR 0..16).
#
# ВАЖНО: пакет 'tinkoff-investments' СНЯТ С PyPI (после архивирования репо
# в октябре 2025). Ставим напрямую с GitHub по тегу. Последний релиз —
# 0.2.0-beta117. Имя пакета после установки — `tinkoff-investments`,
# импортируется как `tinkoff.invest` (PEP 420 namespace).
#
# Совместимость на 2026-05: pin в pyproject.toml SDK устарел (`protobuf<5`),
# но runtime работает с protobuf 5/6 и Python 3.14 при принудительном
# обновлении protobuf — см. строку ниже.
tinkoff-investments @ git+https://github.com/RussianInvestments/invest-python.git@0.2.0-beta117
# Зависимости SDK, явно прописаны (т.к. ставим SDK с --no-deps в production
# не имеет смысла, но фиксируем версии для воспроизводимости):
cachetools>=5.2.0,<6.0.0
deprecation>=2.1.0,<3.0.0
grpcio>=1.59.3,<2.0.0
# protobuf 4.x НЕ работает с Python 3.14 (TypeError: Metaclasses with custom
# tp_new). Берём 5.27+ — runtime SDK с этим совместим, несмотря на устаревший
# pin в его pyproject.
protobuf>=5.27.0,<7.0.0

# Resilience: retry, circuit breaker, rate limiter (in-process до Redis-PR).
tenacity>=9.0.0,<10.0.0
pybreaker>=1.2.0,<2.0.0
aiolimiter>=1.1.0,<2.0.0
# Структурное логирование с contextvars для per-sync correlation_id.
structlog>=24.0.0,<26.0.0

# ==================== AI (OPTIONAL) ====================
# openai>=1.12.0,<2.0.0  # Uncomment if using AI features

# ==================== TESTING ====================
pytest>=8.0.0,<9.0.0
pytest-asyncio>=0.23.0,<0.24.0
pytest-cov>=4.1.0,<5.0.0
httpx>=0.26.0  # For TestClient

# ==================== DEVELOPMENT ====================
# ruff>=0.2.0  # Linting
# black>=24.1.0  # Formatting
